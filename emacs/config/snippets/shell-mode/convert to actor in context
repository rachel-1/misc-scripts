# -*- mode: snippet -*-
# name: convert to actor in context
# key: aic
# --
bazel build //perception/actor_analysis:extract_actor_in_context && ./bazel-bin/perception/actor_analysis/extract_actor_in_context ${1:logid}